@model LifeManagement.Models.DB.Task

@{
    ViewBag.Title = "Delete";
}
@using LifeManagement.Resources;

<h3>@ResourceScr.DeleteQustion @ResourceScr.task "@Model.Name" @ResourceScr.from "@Model.Project.Name" ?</h3>
<div>
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="buttonbar">
            <input type="submit" value="@ResourceScr.Delete" class="btn btn-default fullsize" />
        </div>
    }
</div>
