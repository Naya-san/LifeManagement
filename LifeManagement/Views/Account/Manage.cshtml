@using LifeManagement.Resources
@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = @User.Identity.GetUserName();
}
<div class="changepassword"></div>
<div class="LMсol-md-8">
<h2>@ResourceScr.YouLogged <strong>@User.Identity.GetUserName()</strong>.</h2>

<p class="text-success">@ViewBag.StatusMessage</p>
<div class="rowLM">
    <div class="changepasswordWidth">
        @if (ViewBag.HasLocalPassword)
        {
            @Html.Partial("_ChangePasswordPartial")
        }
        else
        {
            @Html.Partial("_SetPasswordPartial")
        }

        @Html.ActionLink(ResourceScr.back, "Index", "Cabinet")

        @*<section id="externalLogins">
                        @Html.Action("RemoveAccountList")
                        @Html.Partial("_ExternalLoginsListPartial", new { Action = "LinkLogin", ReturnUrl = ViewBag.ReturnUrl })
                    </section>
                </div>*@
    </div>
</div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
